docker run -it -v $HOME/.dockerpi/2019-09-26-raspbian-buster-lite.img:/sdcard/filesystem.img lukechilds/dockerpi
docker run -it -p 127.0.0.1:5022:5022 -p 127.0.0.1:8000:8000 -v $HOME/.dockerpi/2019-09-26-raspbian-buster-lite.img:/sdcard/filesystem.img lukechilds/dockerpi
sshfs pi@localhost:/home/pi/Source/air_quality_monitoring /home/lukas/Source/air_quality_monitoring -p 5022 -o nonempty
sudo apt-get update
sudo raspi-config
sudo apt-get -y install python3-pip
pip3 install smbus2
pip3 install django
docker run --device /dev/i2c-0 --device /dev/i2c-1 myimage

sudo apt-get update
sudo apt-get upgrade
sudo apt-get install git
curl -sSL https://get.docker.com | sh
sudo usermod -aG docker pi
sudo apt-get install python3-pip
sudo pip3 install docker-compose
sudo systemctl enable docker
sudo reboot

gpg: WARNING: unsafe ownership on homedir '/home/pi/.gnupg'


touch wpa_supplicant.conf
country=DE
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
network={
  ssid="NETWORK_NAME"
  psk="NETWORK_PASSWORD"
  key_mgmt=WPA-PSK
}
touch ssh
